#include <stdio.h>

int main(void)
{
    int arr(1)(3)(3);
    int x,o,j,y;
    
    i = 0;
    for(x=0;x<9;x++)
        for(o=0;o<3;o++)
            for(j=0;j<3;j++)
                array[x][o] = i++;
                
                return(0)
}